version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "Construyendo la app Client..."
      - npx turbo run build --filter=client

artifacts:
  base-directory: apps/client/dist
  files:
    - '**/*'