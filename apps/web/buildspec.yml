version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npx turbo run build --filter=web

artifacts:
  base-directory: 'apps/web/dist'
  files:
    - '**/*'