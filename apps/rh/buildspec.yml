version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  build:
    commands:
      - npx turbo run build --filter=rh

artifacts:
  base-directory: 'apps/rh/out'
  files:
    - '**/*'