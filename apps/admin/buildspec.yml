version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "Construyendo solo la app Admin..."
      - npx turbo run build --filter=admin
# ELIMINAMOS LA SECCIÓN POST_BUILD QUE TIENE EL AWS S3 SYNC

artifacts:
  base-directory: apps/admin/dist
  files:
    - '**/*'