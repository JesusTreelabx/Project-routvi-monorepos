version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "Construyendo la app Admin..."
      - npx turbo run build --filter=admin

artifacts:
  base-directory: 'apps/admin/dist' # Asegúrate de que tenga las comillas simples
  files:
    - '**/*'
  discard-paths: yes