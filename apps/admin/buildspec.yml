version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "Construyendo la app..."
      - npx turbo run build --filter=admin

artifacts:
  files:
    - '**/*'
  base-directory: 'apps/admin/dist'